# Configurações do InfluxDB Cloud
# IMPORTANTE: Copie este arquivo para .env e preencha com suas credenciais reais
# NÃO COMMITE o arquivo .env no Git (ele está no .gitignore)

# URL do InfluxDB Cloud
# Exemplo: https://us-east-1-1.aws.cloud2.influxdata.com
INFLUXDB_URL=https://seu-region.aws.cloud2.influxdata.com

# Token de autenticação do InfluxDB
# Obtenha em: InfluxDB Cloud > Load Data > API Tokens
INFLUXDB_TOKEN=seu_token_aqui

# Nome da sua organização no InfluxDB
INFLUXDB_ORG=sua_organizacao

# Nome do bucket onde os dados serão salvos
# Crie um bucket chamado "robo_espacial" no InfluxDB Cloud
INFLUXDB_BUCKET=robo_espacial

# Porta do servidor Flask (padrão: 5000)
PORT=5000
